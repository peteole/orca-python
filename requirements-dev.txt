# Development dependencies for ORCA Python package
# Install with: uv pip install -r requirements-dev.txt

# Build tools
build>=0.10.0
setuptools>=45
wheel>=0.40.0
pybind11>=2.6.0

# Testing
pytest>=7.0.0

# Publishing
twine>=4.0.0
cibuildwheel>=2.16.0

# Core dependencies (also in setup.py/pyproject.toml)
numpy>=1.15.0
